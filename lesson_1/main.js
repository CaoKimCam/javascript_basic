//HTML DOM

// 1. Element
// 2. Attribute
// 3. Text


// Javascript: cung cấp bộ công cụ truy xuất vào HTML DOM
//Javascript: Browser | Server (NodeJS)
// Application programming interface (API)
//Browser: HTML cần access -> DOM, do đó cần-> DOM API

//DOM API này thuộc WEB API (web api này có nhiều, ví dụ lấy vị trí, nhận thông báo hay không?), chỉ có trên nền tảng hỗ trợ mở trang web
//Trên server không có, chỉ lưu trữ và xử lí, do đó trên javascript cũng không có, chỉ có browser có hỗ trợ chạy


